<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.vmall.mapper.questionmapper.QuesUserMapper">
    <insert id="insert">
        insert into v_question_user (v_userId, v_questionId, v_answer) values (#{userId},#{quesId},#{answer});
    </insert>
</mapper>