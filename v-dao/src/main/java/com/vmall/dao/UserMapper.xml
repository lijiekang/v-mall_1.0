<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.vmall.dao.UserMapper">
    <select id="login" resultType="com.vmall.pojo.VUesr">
        select * from v_user where v_usercode=#{userCode} and v_password=#{password};
    </select>

    <update id="updateUserByGrade">
        update v_user set v_grade=#{v_grade}+#{grade}
            where v_userId=#{v_userId}
    </update>

    <select id="getUserIdByUserName" resultType="com.vmall.pojo.VUesr">
        select v_userId from v_user where v_username=#{userName}
    </select>
</mapper>